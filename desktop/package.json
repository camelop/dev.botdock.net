{
  "name": "botdock-desktop",
  "version": "0.0.0",
  "private": true,
  "description": "Tauri shell that bundles the BotDock CLI binary as a sidecar.",
  "scripts": {
    "prepare-sidecar": "bash ./scripts/prepare-sidecar.sh",
    "tauri": "tauri",
    "dev": "npm run prepare-sidecar && tauri dev",
    "build": "npm run prepare-sidecar && tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
